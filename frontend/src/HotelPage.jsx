const HotelPage = ({ id }) => {
    return (
      <div>
        <h1>Hotel Details for {id}</h1>
        {/* Display detailed hotel info here */}
      </div>
    );
  };
  
  export default HotelPage;
  